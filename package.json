{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna exec --no-bail -- standard",
    "test": "lerna run --no-bail test"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "standard": "^16.0.3"
  }
}
